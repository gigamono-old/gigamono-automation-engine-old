syntax = "proto3";
option go_package = "github.com/sageflow/sageengine/internal/proto";


message Message {
    string content = 1;
}

message Empty {}

service EngineService {
    rpc SayHello(Message) returns (Message);
}
